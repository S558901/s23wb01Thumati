extends layout

block content
  h1= title
  p Welcome back to #{title} modifed by Sree Chetan Reddy Thumati
  p
    -let image_names  =["highend.jpg","basic.jpg","small.jpg","4.tel.jpg","5.tel.jpg"]
    -for(var i=0;i<3; i++) {
        -let r = Math.floor(Math.random()*5);
        img(src=`/images/${image_names[r]}` alt=`${r}` style+'height:300px; width:300px')
    -}